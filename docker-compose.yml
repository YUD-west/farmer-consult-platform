services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: yegnafarm
    ports:
      - "5432:5432"
    volumes:
      - yegnafarm_pg:/var/lib/postgresql/data

volumes:
  yegnafarm_pg:
